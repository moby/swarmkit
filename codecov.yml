comment:
  layout: header, changes, diff, sunburst
coverage:
  status:
    patch: false
    project:
      default:
        enabled: yes
        target: 0
    changes: false
  ignore:  # ignore all protobuf-generated code
    - "*.pb.go"
    - "vendor/*"
