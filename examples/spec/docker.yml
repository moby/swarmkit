version: '3'
namespace: app

services:
  web:
    image: nginx
    instances: 3
    mode: running

  redis:
    image: redis
    instances: 1
    mode: running

  ping:
      image: alpine
      command: ["sh", "-c", "ping $HOST"]
      env:
          - HOST=google.com
      instances: 2
      mode: running
